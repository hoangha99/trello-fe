import React from 'react';
import LoginUrl from '../router/LoginUrl';

function Login_register(props) {
    return (
        <div>
            <LoginUrl/>
        </div>
    );
}

export default Login_register;